<%= form_with(model: coffee_review, local: true) do |form| %>
  <% if coffee_review.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(coffee_review.errors.count, "error") %> prohibited this coffee_review from being saved:</h2>

      <ul>
        <% coffee_review.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :coffee_id %>
    <%= form.number_field :coffee_id %>
  </div>

  <div class="field">
    <%= form.label :review %>
    <%= form.text_area :review %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
